Thank you for installing {{ .Chart.Name }}!

Your phpMyAdmin instance has been deployed successfully.

1. Get the application URL:
{{- if .Values.ingress.enabled }}
{{- range $host := .Values.ingress.hosts }}
  {{- range .paths }}
  http{{ if $.Values.ingress.tls }}s{{ end }}://{{ $host.host }}{{ .path }}
  {{- end }}
{{- end }}
{{- else if contains "NodePort" .Values.service.type }}
  export NODE_PORT=$(kubectl get --namespace {{ .Release.Namespace }} -o jsonpath="{.spec.ports[0].nodePort}" services {{ include "phpmyadmin.fullname" . }})
  export NODE_IP=$(kubectl get nodes --namespace {{ .Release.Namespace }} -o jsonpath="{.items[0].status.addresses[0].address}")
  echo "phpMyAdmin is available at http://$NODE_IP:$NODE_PORT"
{{- else if contains "LoadBalancer" .Values.service.type }}
  NOTE: It may take a few minutes for the LoadBalancer IP to be available.
        You can watch the status by running: kubectl get --namespace {{ .Release.Namespace }} svc -w {{ include "phpmyadmin.fullname" . }}
  export SERVICE_IP=$(kubectl get svc --namespace {{ .Release.Namespace }} {{ include "phpmyadmin.fullname" . }} --template "{{"{{ range (index .status.loadBalancer.ingress 0) }}{{.}}{{ end }}"}}")
  echo "phpMyAdmin is available at http://$SERVICE_IP:{{ .Values.service.port }}"
{{- else if contains "ClusterIP" .Values.service.type }}
  kubectl port-forward --namespace {{ .Release.Namespace }} svc/{{ include "phpmyadmin.fullname" . }} 8080:{{ .Values.service.port }} &
  echo "phpMyAdmin is available at http://127.0.0.1:8080"
{{- end }}

2. Database Connection:

  phpMyAdmin is configured to connect to:
  - Host: {{ .Values.phpmyadmin.dbHost }}
  - Port: {{ .Values.phpmyadmin.dbPort }}
{{- if eq (int .Values.phpmyadmin.arbitrary) 1 }}

  NOTE: Arbitrary server connection is ENABLED. You can connect to any MySQL/MariaDB
  server by entering the hostname in the phpMyAdmin login screen.
{{- end }}

3. To log in to phpMyAdmin, you will need:
   - A valid MySQL/MariaDB username and password for the database server
   - If using a MariaDB chart deployed in the same cluster, retrieve credentials with:

     kubectl get secret --namespace {{ .Release.Namespace }} <mariadb-release-name> -o jsonpath="{.data.mariadb-root-password}" | base64 -d

     (Replace <mariadb-release-name> with the actual name of your MariaDB release, e.g., "my-mariadb")

For more information, visit the phpMyAdmin documentation at https://www.phpmyadmin.net/docs/
